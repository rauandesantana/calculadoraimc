import 'package:flutter/cupertino.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:calculadoraimc/main.dart';

void main() {
  testWidgets('Checar Estado dos Componentes',
      (WidgetTester tester) async {
    //await tester.pumpWidget(const MyApp());

    //const keyValueImc = Key("valueIMC");
    //const keyValueMessage = Key("valueMessage");
    //const keyFieldName = Key("fieldName");
    //const keyFieldHeight = Key("fieldHeight");
    //const keyFieldWeight = Key("fieldWeight");
    //const keyCalculateAndSaveButton = Key("calculateAndSaveButton");

    //final valueIMC = find.byKey(keyValueImc);
    //final valueMessage = find.byKey(keyValueMessage);
    //final fieldName = find.byKey(keyFieldName);
    //final fieldHeight = find.byKey(keyFieldHeight);
    //final fieldWeight = find.byKey(keyFieldWeight);
    //final calculateAndSaveButton = find.byKey(keyCalculateAndSaveButton);

    //expect(valueIMC, findsOneWidget);
    //expect(valueMessage, findsOneWidget);
    //expect(fieldName, findsOneWidget);
    //expect(fieldHeight, findsOneWidget);
    //expect(fieldWeight, findsOneWidget);
    //expect(calculateAndSaveButton, findsOneWidget);

    //await tester.enterText(fieldName, "Test Name");
    //await tester.enterText(fieldHeight, "1.65");
    //await tester.enterText(fieldWeight, "88.6");
    //await tester.tap(calculateAndSaveButton);
    //await tester.pump();

    //expect(find.text('32.54'), findsNWidgets(2));
    //expect(find.text('Obesidade Grau I'), findsNWidgets(2));
    //expect(find.text('Test Name'), findsNWidgets(2));
    //expect(find.text('1.65'), findsOneWidget);
    //expect(find.text('88.6'), findsOneWidget);
  });
}
